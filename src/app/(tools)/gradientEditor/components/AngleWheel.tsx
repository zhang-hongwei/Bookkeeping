/**
 * Re-export shared AngleWheel for the gradient editor.
 * The SVG-based shared version replaces this local CSS-based duplicate.
 */
export { default } from '@/components/shared/color-stop/AngleWheel';
